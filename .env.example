# Copy to .env and fill in your keys
ANTHROPIC_API_KEY=sk-ant-...

# USDA FoodData Central key (https://fdc.nal.usda.gov/api-key-signup).
# Optional: falls back to the shared DEMO_KEY (low rate limit) if left blank.
FDC_API_KEY=
